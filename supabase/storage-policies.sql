-- Create bucket 'evidence' manually in Storage UI, then apply policies as needed.
-- Example policies (adjust to your needs):

-- Allow authenticated users to upload to evidence/ORG_ID/*
-- NOTE: Supabase storage RLS uses separate policy system; this is indicative only.
