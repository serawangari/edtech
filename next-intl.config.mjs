// next-intl.config.mjs (root)
export default {
  // Your three locales:
  locales: ['en', 'fr', 'sw'],
  // Default:
  defaultLocale: 'en',
  // Keep urls clean like /en/... only when needed
  localePrefix: 'as-needed'
};

